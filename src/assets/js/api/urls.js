/**
 * url : {
 *      method: post || get || delete || put,
 *      url: url address
 * }
 */
const urls = {
    // configs
    configs: {
        method: 'get',
        url: '/platform/configs'
    },
    // account
    accountCount: {
        method: 'post',
        url: '/account/count'
    }
}

export default urls